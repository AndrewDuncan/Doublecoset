 number_of_edges=0
    for u in stall.vertices:
        for e in u.outedgesList:
            number_of_edges+=1

            if 


nr=1
            while nr==1:
            nr=build_new_labelling(stall,FZ)
            if nr==1:
                print("The generators input for ", Hname, " are not Nielsen reduced")
                print("Please enter a Nielsen reduced basis")
                n=enter_positive_integer("the number of  generators of the subgroup: ")
                (H,FZ)=subgroup_input_and_compute(Hname,n) 
         
